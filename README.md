# TOEIC Speaking Review

Open `index.html` locally, or upload this whole folder to a static website host.

Required files:
- `index.html`
- `toeic_review_data.js`
- `images/`

For mobile sharing, upload the whole `toeic_review_mobile` folder to a web host and share the resulting URL.
